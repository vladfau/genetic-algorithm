#pragma once
#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <inttypes.h>

#define MAX_GENOME 20

uint8_t randr(uint8_t min, uint8_t max);

char inverse(char ch);

float getChance();


